# 更新日志

## [0.0.1] - 2025-01-01

### 新增
- 项目初始化，完成微软拼音自定义短语管理工具的核心功能。
- 命令行工具 `pinyin_phrase_tool.py` 支持导入、导出、列出短语。
- 图形界面 `pinyin_phrase_gui.py` 提供导入、导出、列出、备份、编辑短语等交互操作。
- 支持 `--force` 和 `--dry-run` 参数。
- 导入自动跳过注释行和格式错误行，自动分配序号。
- 导入前自动备份词库；GUI 中提供独立备份按钮。
- 新增 `phrases.txt` 明文编辑文件，方便手动编辑后导入。